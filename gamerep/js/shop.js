import {EQUIPMENT,CLOTHES} from "./economy.js";
export function shopHTML(s){const cats=[["mic","Микрофоны"],["headphones","Наушники"],["pc","ПК"],["camera","Камеры"]];return `<h1>МАГАЗИН</h1><div class="shop-grid">${cats.flatMap(([type,title])=>Object.entries(EQUIPMENT[type]).map(([id,e])=>`<div class="card ${s.equipment[type]===id?"owned":""}"><div class="rarity">${title}</div><h3>${e.name}</h3><div class="price">₽ ${e.price.toLocaleString("ru-RU")}</div><div class="stat">Качество/бонус: +${e.quality||e.speed||e.stream||0}%</div><button data-buy-equip="${type}:${id}" ${s.equipment[type]===id||s.money<e.price?"disabled":""}>${s.equipment[type]===id?"УСТАНОВЛЕНО":"КУПИТЬ"}</button></div>`)).join("")}</div><h2>STYLE</h2><div class="shop-grid">${CLOTHES.map(c=>`<div class="card ${s.clothes.includes(c[0])?"owned":""}"><div class="rarity">STYLE</div><h3>👕 ${c[1]}</h3><div class="price">₽ ${c[2].toLocaleString("ru-RU")}</div><div class="stat">Стиль +${c[3]} • Популярность +${c[4]}%</div><button data-buy-cloth="${c[0]}" ${s.clothes.includes(c[0])||s.money<c[2]?"disabled":""}>${s.clothes.includes(c[0])?"КУПЛЕНО":"КУПИТЬ"}</button></div>`).join("")}</div><button data-close-panel>ЗАКРЫТЬ</button>`}