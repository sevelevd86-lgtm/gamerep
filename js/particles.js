export class Particles{constructor(){this.a=[]}burst(x,y,color="#fff",n=16,power=5){for(let i=0;i<n;i++){const t=Math.random()*Math.PI*2,v=Math.random()*power;this.a.push({x,y,vx:Math.cos(t)*v,vy:Math.sin(t)*v,life:.5+Math.random()*.6,color})}}update(dt){for(const p of this.a){p.x+=p.vx;p.y+=p.vy;p.vy+=.12;p.life-=dt}this.a=this.a.filter(p=>p.life>0)}draw(ctx){for(const p of this.a){ctx.globalAlpha=Math.max(0,p.life);ctx.fillStyle=p.color;ctx.fillRect(p.x,p.y,4,4)}ctx.globalAlpha=1}}